"use client";

import React from "react";

export default function FocusTimer() {
  // 简单占位实现，后续再做真正的番茄钟逻辑
  return (
    <div className="border rounded-lg p-3 text-sm bg-neutral-900/40 text-neutral-100">
      <div className="font-semibold mb-1">专注计时器</div>
      <div className="text-xs opacity-70">
        这里是占位组件。后续可以接入真正的计时功能。
      </div>
    </div>
  );
}
